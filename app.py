import streamlit as st

# 1. 設定網頁標題 (這行必須放在最上面)
st.set_page_config(page_title="永續城市預測平台", page_icon="🌏")

# 2. 建立側邊欄與選單 (Sidebar)
with st.sidebar:
    st.header("功能導覽")
    # 這裡使用 radio button 來做頁面切換開關
    page = st.radio("請選擇頁面", ["專案總覽", "縣市預測"])
    
    st.divider()
    st.write("大數據分析期末專案")
    st.write("組員：人5")

# 3. 根據選單的選擇，顯示不同的內容
if page == "專案總覽":
    # --- 這是總覽頁面 ---
    st.title("專案總覽：永續城市與氣候行動 🏙️")
    st.info("本專案旨在透過數據分析，探討城市發展與氣候變遷的關聯。")

    st.subheader("我們關注的聯合國永續發展目標 (SDGs)")
    
    # 建立兩欄來放圖片
    col1, col2 = st.columns(2)
    
    with col1:
        # 新的 SDG 11 圖片連結 (已修正)
        st.image("https://sdgs.un.org/sites/default/files/goals/E_SDG_goals_icons-individual-rgb-11.png", 
                 caption="SDG 11: 永續城鄉")

    with col2:
        # 新的 SDG 13 圖片連結 (已修正)
        st.image("https://sdgs.un.org/sites/default/files/goals/E_SDG_goals_icons-individual-rgb-13.png", 
                 caption="SDG 13: 氣候行動")

elif page == "縣市預測":
    # --- 這是縣市預測頁面 ---
    st.title("縣市數據預測分析 📊")
    st.warning("🚧 這個頁面正在施工中...")
    
    # 這裡先放一些假裝的功能，讓它看起來很厲害
    city = st.selectbox("請選擇要分析的縣市", ["台北市", "新北市", "桃園市", "台中市", "高雄市"])
    st.write(f"您目前選擇的縣市是：**{city}**")
    
    st.write("未來這裡會放入機器學習模型的預測結果。")
